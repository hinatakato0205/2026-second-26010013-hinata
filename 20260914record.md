なぜかリポジトリを作らされました。後期も頑張るぞ

何をさせられるのか、不安です。

リモート側で動きを出しました。

ローカル側で編集をします。下記のgitコマンドを記入しました。

git pull

git push

git add .

git commit -m "message"

git status

Rushをインストールした

rustcコマンドを使った

.gitignoreファイルを作った
/target

rustで数当てゲームをちょっと作った
知らない書き方でむずかしかった